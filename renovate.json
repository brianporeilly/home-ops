{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "github>home-operations/renovate-presets#8.1.0",
    "github>home-operations/renovate-presets//apps/cnpg.json5#8.1.0",
    "github>home-operations/renovate-presets//apps/searxng.json5#8.1.0",
    "local>brianporeilly/home-ops//.github/renovate/automerge.json5"
  ],
  "labels": ["dependencies"],
  "flux": {
    "managerFilePatterns": [
      "/kubernetes/apps/.+\\.yaml$/"
    ]
  },
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": ["/^\\.github/workflows/.+\\.ya?ml$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>.+?) depName=(?<depName>.+?)\\s+[A-Z_]+_VERSION: (?<currentValue>.+)"
      ],
      "datasourceTemplate": "{{{datasource}}}",
      "depNameTemplate": "{{{depName}}}"
    }
  ],
  "packageRules": [
    {
      "description": "Group app-template chart updates",
      "matchPackageNames": ["ghcr.io/bjw-s-labs/helm/app-template"],
      "groupName": "app-template",
      "automerge": false
    },
    {
      "description": "Group kube-prometheus-stack with its companion CRDs chart - they're versioned independently but released in lockstep against the same prometheus-operator appVersion",
      "matchPackageNames": [
        "ghcr.io/prometheus-community/charts/kube-prometheus-stack",
        "ghcr.io/prometheus-community/charts/prometheus-operator-crds"
      ],
      "groupName": "kube-prometheus-stack",
      "automerge": false
    },
    {
      "description": "Group mariadb-operator with its companion CRDs chart - previously merged as separate PRs (#58/#60, #87/#89)",
      "matchPackageNames": ["mariadb-operator", "mariadb-operator-crds"],
      "groupName": "mariadb-operator",
      "automerge": false
    },
    {
      "description": "Paused 2026-08-22 - 26.7.0 crash-loops on this cluster's host-managed-driver setup (see the comment next to the version pin in nvidia-gpu-operator/app/ocirepository.yaml for the full writeup). Re-enable once root-caused, don't just remove this rule without re-testing.",
      "matchPackageNames": ["ghcr.io/home-operations/charts-mirror/nvidia-gpu-operator"],
      "enabled": false
    },
    {
      "description": "Paused 2026-09-02 - v0.5.12's frontend can't render on a cold/logged-out session, blocking login entirely (see the comment next to the version pin in tube-archivist/app/helmrelease.yaml for the full writeup). Re-enable once fixed upstream.",
      "matchPackageNames": ["docker.io/bbilly1/tubearchivist"],
      "enabled": false
    }
  ]
}
