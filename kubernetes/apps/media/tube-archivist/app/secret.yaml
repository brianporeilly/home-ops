---
apiVersion: v1
kind: Secret
metadata:
  name: tube-archivist-secret
stringData:
  # Initial TubeArchivist superuser password (username is set via TA_USERNAME env)
  TA_PASSWORD: changeme
  # Must match between the tubearchivist app and the elasticsearch container
  ELASTIC_PASSWORD: changeme
