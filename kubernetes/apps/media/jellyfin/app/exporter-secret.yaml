---
# NOT sops-encrypted yet - replace the placeholder below with a real API key
# (Jellyfin admin dashboard -> Dashboard -> API Keys -> +), then run
# `sops --encrypt --in-place exporter-secret.yaml` before committing.
apiVersion: v1
kind: Secret
metadata:
  name: jellyfin-exporter-secret
stringData:
  JELLYFIN_TOKEN: "REPLACE_ME"
