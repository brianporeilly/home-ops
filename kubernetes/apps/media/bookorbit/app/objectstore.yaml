---
# Barman Cloud Plugin backup target (Ceph RGW S3). Credentials come from the
# ObjectBucketClaim secret (obc.yaml). See docs/backup-dr-plan.md.
apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: bookorbit-postgres-backup
spec:
  retentionPolicy: 14d
  configuration:
    destinationPath: s3://cnpg-bookorbit/
    endpointURL: http://rook-ceph-rgw-ceph-objectstore.rook-ceph.svc
    s3Credentials:
      accessKeyId:
        name: bookorbit-postgres-backup
        key: AWS_ACCESS_KEY_ID
      secretAccessKey:
        name: bookorbit-postgres-backup
        key: AWS_SECRET_ACCESS_KEY
    wal:
      compression: gzip
    data:
      compression: gzip
