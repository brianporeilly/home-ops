# sops --encrypt secret.yaml
apiVersion: v1
kind: Secret
metadata:
    name: kopiur-repository-secret
stringData:
    # Filesystem/NFS backends need only the repository encryption password -
    # the data sits on the NAS but kopia still encrypts it. No object-store
    # keys for this backend.
    KOPIA_PASSWORD: yvBCWPZ307oGB8h96iasrxVfWLY1ZGCiOBQR2VQa
