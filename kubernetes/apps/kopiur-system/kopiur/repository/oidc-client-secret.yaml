# sops --encrypt --in-place oidc-client-secret.yaml
---
apiVersion: v1
kind: Secret
metadata:
  name: kopiur-ui-oidc-client-secret
stringData:
  # Same value as kubernetes/apps/authentik/authentik/app/kopiur-ui-provider-secret.yaml's
  # KOPIUR_UI_OAUTH_CLIENT_SECRET - duplicated, not cross-referenced
  # (different namespace, different consumer). Key name is literally
  # "client-secret", per Envoy Gateway's SecurityPolicy.oidc.clientSecret
  # convention (same as echo's).
  client-secret: "539e163da51ac7844e83e106cacc6ee5ac91053bdd00a87fdc4fc82e7daf79de7eda7c6c357bd877"
