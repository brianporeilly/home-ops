---
apiVersion: clickhouse.altinity.com/v1
kind: ClickHouseInstallation
metadata:
  name: akvorado-clickhouse
spec:
  configuration:
    clusters:
      - name: akvorado
        layout:
          shardsCount: 1
          replicasCount: 1
    users:
      akvorado/password:
        valueFrom:
          secretKeyRef:
            name: akvorado-creds
            key: CLICKHOUSE_PASSWORD
      akvorado/networks/ip: "::/0"
      akvorado/profile: default
  templates:
    podTemplates:
      - name: akvorado-clickhouse
        spec:
          containers:
            - name: clickhouse
              image: clickhouse/clickhouse-server:26.8@sha256:795120c18a0022452ac3f463809d307a73c285b17d969734af12f8d2eff4646a
              resources:
                requests:
                  cpu: 500m
                  memory: 1Gi
                limits:
                  memory: 4Gi
    volumeClaimTemplates:
      - name: akvorado-clickhouse-storage
        spec:
          storageClassName: ceph-block
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 50Gi
  defaults:
    templates:
      podTemplate: akvorado-clickhouse
      dataVolumeClaimTemplate: akvorado-clickhouse-storage
