# sops --encrypt secret.yaml
apiVersion: v1
kind: Secret
metadata:
    name: crowdsec-secret
stringData:
    # Machine credentials registered directly against OPNsense's LAPI via
    # `cscli machines add k8s-envoy-log-processor --password ... -f -`
    # (run locally on the LAPI host, not through this cluster) - not
    # self-registered by this pod, so no LAPI-side approval step needed.
    AGENT_USERNAME: k8s-envoy-log-processor
    AGENT_PASSWORD: Kx9mP2vLqR8wZnT4fJhY6dCsA3eG7bNu
