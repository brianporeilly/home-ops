---
apiVersion: v1
kind: Secret
metadata:
  name: arr-notifications-creds
stringData:
  # TODO: fill in your real media-notifications Slack webhook before encrypting.
  # This is deliberately separate from any other Slack webhook already used
  # elsewhere in the cluster (alerting, gatus, etc.).
  SLACK_WEBHOOK_URL: https://hooks.slack.com/services/CHANGEME/CHANGEME/CHANGEME
  # TODO: generate via Jellyfin's own Dashboard > API Keys > "+" and paste
  # here before encrypting - can't be bootstrapped, Jellyfin already has a
  # real admin account set up.
  JELLYFIN_API_KEY: CHANGEME
