---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./envoy.yaml
  - ./observability.yaml
# envoy-patch-error-pages-status-code.yaml deliberately NOT listed here -
# drafted and validated, not adopted. See that file's own header comment
# and docs/error-pages-plan.md.
