# sops --encrypt --in-place oidc-client-secret.yaml
---
apiVersion: v1
kind: Secret
metadata:
  name: echo-oidc-client-secret
stringData:
  # Same value as kubernetes/apps/authentik/authentik/app/echo-provider-secret.yaml's
  # ECHO_OAUTH_CLIENT_SECRET - duplicated, not cross-referenced (different
  # namespace, different consumer). Key name is literally "client-secret",
  # per Envoy Gateway's own SecurityPolicy.oidc.clientSecret convention
  # (confirmed against envoyproxy/gateway's own OIDC docs example).
  client-secret: "e2a3ac0c225cf50126a64c9f2d042a1faf34fec0257a431c81d3573c0d1d571868fca972c49f06f7"
