# sops --encrypt --in-place echo-provider-secret.yaml
---
apiVersion: v1
kind: Secret
metadata:
  name: authentik-echo-provider-secret
stringData:
  # Same value as kubernetes/apps/network/echo/app/oidc-client-secret.yaml -
  # duplicated (not cross-referenced) since each is read by a different
  # system in a different namespace: this one by authentik's OAuth2Provider
  # blueprint (via !Env), the other by Envoy Gateway's SecurityPolicy.
  ECHO_OAUTH_CLIENT_ID: "ccf8e9cebe8abbcfd1ba325730f1db15371be26c"
  ECHO_OAUTH_CLIENT_SECRET: "e2a3ac0c225cf50126a64c9f2d042a1faf34fec0257a431c81d3573c0d1d571868fca972c49f06f7"
