---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: home
components:
  - ../../components/common
resources:
  - ./home-assistant/ks.yaml
  # paperless-ngx: disabled until secret.yaml is sops-encrypted with PAPERLESS_SECRET_KEY
  # - ./paperless-ngx/ks.yaml
  #- ./frigate/ks.yaml
# - ./grocy/ks.yaml
